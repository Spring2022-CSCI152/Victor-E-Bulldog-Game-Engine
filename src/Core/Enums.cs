namespace Bulldog.Core;

public enum RenderTarget
{
    Deferred,
    Forward,
    
}

public enum TextureTarget
{
    
}

public enum LightingType
{
    Point = 1,
    Directional = 0,
    Spot = 2,
    SoftSpot = 3
}

public enum Axis{
    X, Y ,Z
}