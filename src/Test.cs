using SDL2;
using System;

namespace Bulldog
{
    
public class Test : Application
{
    public Test(string initialWindowTitle, int initialWindowHeight, int initialWindowWidth, SDL.SDL_WindowFlags flags) : base(initialWindowTitle, initialWindowHeight, initialWindowWidth, flags)
    {
    }

    protected override void Initialize()
    {
    }

    protected override void LoadContent()
    {
    }

    protected override void Update()
    {
    }

    protected override void Render()
    {
    }
}
}